# PostgreSQL connection - copy to .env and update if needed
# Example for local docker-compose (the compose file uses this connection):
DATABASE_URL=postgres://postgres:postgres@db:5432/shoplens

# Optional: maximum concurrency for Playwright
MAX_CONCURRENCY=2
